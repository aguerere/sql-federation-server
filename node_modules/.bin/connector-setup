#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../connector-setup/bin/connector-setup" "$@"
  ret=$?
else 
  node  "$basedir/../connector-setup/bin/connector-setup" "$@"
  ret=$?
fi
exit $ret
